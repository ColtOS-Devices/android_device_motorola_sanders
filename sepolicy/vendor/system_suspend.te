#allow system_suspend init:binder { transfer };
#allow system_suspend init:binder { transfer };

#allow system_suspend init:binder { transfer };

#allow system_suspend init:binder { transfer };

#allow system_suspend init:binder { transfer };
#allow system_suspend init:binder { transfer };
#allow system_suspend init:binder { transfer };
#allow system_suspend init:binder { transfer };

