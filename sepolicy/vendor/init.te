# binder_call(init, mm-qcamerad);
#binder_call(init, hwservicemanager);
# binder_call(init, servicemanager);

#allow init hwservicemanager:binder call;
#allow init mm-qcamerad:binder transfer;
#allow init platform_app:binder transfer;

allow init system_app:binder transfer;
allow init system_data_file:file lock;

allow init audio_device:chr_file { write ioctl };
allow init input_device:chr_file rw_file_perms;
allow init sensors_device:chr_file { write ioctl };
allow init tee_device:chr_file { write ioctl };

allow init servicemanager:binder { transfer call };
allow init system_server:binder { transfer call };

allow init property_socket:sock_file write;
allow init socket_device:sock_file { create setattr unlink };

#allow init system_data_file:file { rename append };
#allow init firmware_file:dir mounton;

#allow init fm_radio_device:chr_file write;

# ptt_socket_app
allow init dnsproxyd_socket:sock_file write;
allow init netd:unix_stream_socket connectto;
allow init self:netlink_socket { read write getattr connect };

allow init debugfs:file write;
#allow init persist_file:filesystem { getattr mount relabelfrom relabelto unmount };

allow init self:capability sys_nice;

#allow init bt_firmware_file:filesystem { associate };
#allow init firmware_file:filesystem { associate };

allow init sensors_device:chr_file { rw_file_perms create };

allow init self:netlink_route_socket { bind create getopt nlmsg_read read setopt write };

allow init self:capability2 { block_suspend };

allow init hal_sensors_hwservice:hwservice_manager find;

allow init { domain -lmkd -crash_dump }:process noatsecure;

#allow init hal_perf_hwservice:hwservice_manager find;
allow init hidl_base_hwservice:hwservice_manager add;

allow init device:chr_file { ioctl  open  write };
allow init init:socket { read  write };
allow init proc:file { write };
allow init sysfs:file { open  read  write };
allow init sysfs_devices_system_cpu:file { write };
#allow init system_server:binder { call };
#allow init system_suspend:binder { call };
allow init vendor_data_file:file { lock };

allow init fwmarkd_socket:sock_file { write };
allow init hwservicemanager:binder { call  transfer };
allow init init:netlink_generic_socket { read  write };
allow init init:netlink_route_socket { getattr  nlmsg_write };
allow init init:socket { connect  create  setopt };
allow init init:udp_socket { ioctl  read  write };
allow init mnt_vendor_file:lnk_file { read };
allow init netutils_wrapper_exec:file { execute  execute_no_trans  getattr  read };
#allow init system_file:file { execute_no_trans };
#allow init system_suspend:binder { call };
allow init vendor_data_file:file { ioctl };
allow init init:binder { call };
allow init init:tcp_socket { bind  create  getattr  getopt  listen  read  setopt  write };
allow init init:udp_socket { bind  getattr  ioctl  setopt };
allow init netutils_wrapper_exec:file { open };
allow init node:tcp_socket { node_bind };
allow init node:udp_socket { node_bind };
allow init port:tcp_socket { name_bind };
allow init port:udp_socket { name_bind };
allow init ram_device:blk_file { write };
allow init socket_device:sock_file { write };
#allow init system_file:file { execute_no_trans };
#allow init system_suspend:binder { call };
allow init vendor_data_file:file { append };
allow init vendor_file:file { execute  execute_no_trans };
allow init vndbinder_device:chr_file { ioctl };

allow init device:chr_file { read };
allow init init:socket { ioctl };
allow init init:udp_socket { ioctl };
allow init port:tcp_socket { name_connect };
#allow init system_file:file { execute_no_trans };
#allow init system_suspend:binder { call };

allow init block_device:blk_file { ioctl };
allow init init:tcp_socket { connect };
allow init init:udp_socket { ioctl };
allow init same_process_hal_file:file { execute };
allow init system_file:file { execute_no_trans };
#allow init system_suspend:binder { call };

allow init block_device:blk_file { ioctl };
#allow init init:udp_socket { ioctl };
#allow init system_suspend:binder { call };

allow init block_device:blk_file { ioctl };
allow init block_device:blk_file { ioctl };
allow init bluetooth:binder { call };
allow init hci_attach_dev:chr_file { ioctl  open  read };
allow init init:udp_socket { ioctl };
allow init sysfs_wake_lock:file { append  open };
#allow init system_suspend:binder { call };

allow init block_device:blk_file { ioctl };
allow init init:binder { transfer };
allow init init:netlink_generic_socket { bind  create };
#allow init init:udp_socket { ioctl };
#allow init system_suspend:binder { call };
allow init vndbinder_device:chr_file { open  read  write };
allow init vndservicemanager:binder { call };

allow init block_device:blk_file { ioctl };
allow init block_device:blk_file { ioctl };
allow init block_device:blk_file { ioctl };
#allow init init:udp_socket { ioctl };
#allow init system_suspend:binder { call };

